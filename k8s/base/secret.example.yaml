# Template only — DO NOT COMMIT REAL VALUES.
#
# Create the real secret out of band:
#
#   kubectl -n fleetops create secret generic fleetops-api-secrets \
#     --from-literal=ConnectionStrings__FleetOpsDb='Host=postgres;Database=fleetops;Username=fleetops;Password=...' \
#     --from-literal=Jwt__Key="$(openssl rand -base64 48)" \
#     --from-literal=Database__DemoPassword='...'
#
# In real AWS, replace this with the Secrets Store CSI driver or External Secrets
# reading from AWS Secrets Manager, with the pod's identity supplied by IRSA.
apiVersion: v1
kind: Secret
metadata:
  name: fleetops-api-secrets
  namespace: fleetops
type: Opaque
stringData:
  ConnectionStrings__FleetOpsDb: 'REPLACE_ME'
  Jwt__Key: 'REPLACE_ME'
  Database__DemoPassword: 'REPLACE_ME'
