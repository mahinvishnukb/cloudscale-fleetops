apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: fleetops

resources:
  - namespace.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - hpa.yaml
  - pdb.yaml

commonLabels:
  app.kubernetes.io/part-of: fleetops
  app.kubernetes.io/managed-by: kustomize
